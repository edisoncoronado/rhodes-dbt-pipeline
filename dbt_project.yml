name: 'rhodes_dbt_pipeline'
version: '1.0'
config-version: 2

profile: 'RHODES_DWH'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
snapshot-paths: ["snapshots"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  rhodes_dbt_pipeline:
    staging:
      +materialized: view
    marts:
      +materialized: table